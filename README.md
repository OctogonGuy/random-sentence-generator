# Random Sentence Generator

Uses random method to randomly generate English grammar based on the main sentence patterns along with optional additions and parts of speech that are filled with randomly selected words corresponding to that part of speech.

## Setup

1. Install Java (Version 25+)
2. Install Maven

## Usage

Run the following:

```
mvn javafx:run
```